import React from "react";
import {connect} from "react-redux";

const CreateCropPattern = props => {
    return(
        <div>
            hello
        </div>
    )
};

const mapStateToProps = state => {
    return {

    }
};

const mapDispatchToProps = dispatch => {
    return {

    }
};

export default connect(mapStateToProps, mapDispatchToProps)(CreateCropPattern);